programa pruebaFrida 

	rutina entero : ret(entero : x){
		imprimir('-----');
		imprimir(x);
		imprimir('-----');
		regresa x;
	}

	lienzo {
		variable entero id2 = 5;
		variable entero arr[1] = {9};

		imprimir(2 + arr[0] * 2);

		imprimir(2 + ret(9) * 2);

	}